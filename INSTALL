To build dbd2netcdf from source, do the following:

A) Clone the source from:

https://github.com/mousebrains/dbd2netcdf

C) cd into the source archive
D) Execute cmake

cmake .

E) Run make to build the software

make

F) Run make to test the software

make check

G) Run make to install the software

make install
